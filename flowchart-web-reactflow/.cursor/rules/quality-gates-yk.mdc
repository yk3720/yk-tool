---
description: flowchart-web-reactflow 品質ゲート — lint/hook/CI
globs: "**/flowchart-web-reactflow/**/*"
alwaysApply: false
---

# Quality Gates — flowchart-web-reactflow

**SSOT:** `c:/yk-skill/rule/60_tooling/QUALITY_GATE_RULES.md`

## scripts（優先使用）

| コマンド | 用途 |
|----------|------|
| `npm run lint` | ESLint |
| `npm run typecheck` | TypeScript |
| `npm run test` | Vitest |
| `npm run excel:test` | pytest |
| `npm run excel:lint` | Ruff |

## pre-commit（pre-commit フレームワーク）

`check-merge-conflict` · `detect-private-key` · ruff（Python）

## hook 失敗時

修正 → 再 commit。`--no-verify` 禁止。秘密検出時は値をチャットに貼らない（`SECRETS_HYGIENE_RULES`）。
