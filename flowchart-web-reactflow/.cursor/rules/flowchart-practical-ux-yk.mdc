---
description: flowchart-web 実用版 UX（3ペイン・ツールバー・警告）— REACTFLOW_RULES §5.6
globs: "**/flowchart-web-reactflow/**/*.{ts,tsx}"
alwaysApply: false
---

# 実用版 UX — flowchart-web-reactflow

**L1 SSOT:** `c:/yk-skill/rule/35_reactflow/REACTFLOW_RULES.md` **§5.6**  
**UX チェックリスト（画面・文言）:** `c:/yk-skill/rule/10_meta/USABILITY_HEURISTICS_RULES.md` No **20**（大きな UI 変更時）
**機能削除:** 同 §5.5 · `ui-simplification-yk.mdc`  
**操作部品 class · workspace 比率 · スタイル索引:** `REACTFLOW_RULES` §5.6-2a · `flowchartUiClasses.ts` · `docs/design-system.md`

## 触る前に確認

| 領域 | やってよい | やらない |
|------|------------|----------|
| Nav | 装置→ユニット→動作 · `moduleDraftKey` | 装置名をヘッダーに二重表示 |
| **装置一括取込** | **その他 → import.json を取込…**（editor · workspace）· **Python 正規化済み JSON のみ** | Web で入力用 xlsx を直接パース · 構成行削除の DB 自動 prune |
| 表（10列） | 段·列·Text1–3·**色**（最右列 · 狭いペインでは**表エリア横スクロール**）· **部分貼り付け**（セル選択 → Ctrl+V · 起点から右・下 · 行不足時は行追加） | 色列を 9 列 UI に畳む · 部分貼り付けで全表置換（それは CsvPastePanel） |
| ツールバー | 再生成/表保存/表読込を常時 · 他はその他メニュー（**始め方 / サンプル（例） / 出力 / 装置取込 / 下書き** · `MenuSection`） | ヘッダーにボタン乱立 |
| レイアウト D | workspace: ツールバーは**表ペイン** · プレビュー列は縦 full-bleed（`fillContainer`） | 全幅ヘッダーがプレビュー上に載る |
| 見た目 | `DEFAULT_LAYOUT` · `flowColors` 固定 · **操作 chrome は `fcBtn*` / `fcTable*`（`flowchartUiClasses.ts`）** · 枠太さは `VISUAL_DESIGN_RULES` §2（2px）· 斜め図形は SVG stroke · 表 ID バッジはプレビューのみ | テーマ/サイズ preset 復活 · `clip-path`+border · chrome に Tailwind 色直書き · export にデバッグ UI |
| 警告 | `WARNING_BANNER_HINT` · 平易な直し方 | 警告で生成停止 |
| ノード文字 | `FlowShapeNode` **`flex-col`** · `measureHeights` | Text1–3 を横並び |

## コード SSOT

| ファイル | 内容 |
|----------|------|
| `lib/flowchart/moduleHierarchy.ts` | 装置一覧 · `moduleDraftKey` · 旧キー |
| `components/flowchart/ModuleNavPane.tsx` | 装置 select |
| `components/flowchart/EditorMoreMenu.tsx` | その他メニュー · セクション IA · **装置取込** · 取込 file input は **`data-testid="import-bundle-file"`**（**メニュー open 時のみ DOM** · 表読込 input と別） |
| `lib/flowchart/actions/importEquipmentBundle.ts` | import.json → RPC `import_equipment_bundle` · **E2E:** `PLAYWRIGHT_E2E=1` + `IMPORT_E2E_STUB=1` 時のみ RPC スキップ（`playwright.config` · 本番・通常 dev 無効） |
| `tools/excel_normalize/` | 入力用 xlsx → import.json（**正規化 SSOT · pytest**）· **xlsx は Git 非追跡** |
| `tools/excel_normalize/fixtures/devices/README.md` | **作者 Excel 置き場 SSOT** · `{社内番号}_{装置名}/` · 現行 `A0001_塗布装置/` |
| `components/flowchart/flowchartUiClasses.ts` | 操作 UI class 正本（`fcBtn*` · `fcTable*` · `fcColorLegend*` · バナー） |
| `components/flowchart/FlowTableEditor.tsx` | 10 列表編集 · **`fcTable*`** · **部分貼り付け**（`onPasteCapture` · `focusCellRef`） |
| `lib/flowchart/pasteTableCells.ts` | 表内部分貼り付け — `parseClipboardGrid` · `applyPartialPaste`（ヘッダー判定なし） |
| `lib/flowchart/parseCsv.ts` | **全表**貼り付け — `parseCsvPaste`（6 列以上 · 1 行目ヘッダー判定）· `CsvPastePanel` 用 |
| `components/flowchart/CsvPastePanel.tsx` | 全表置換（テキストエリア · Excel ファイル） |
| `app/dev/style/` | 開発用スタイルカタログ · `http://localhost:3000/dev/style`（本番 404） |
| `docs/design-system.md` | **スタイルガイド索引（入口 SSOT）** · レイヤー A〜E · Phase 1–2 完了 |
| `tools/excel_normalize/templates/入力用テンプレ_v0.2.xlsx` | 空テンプレ v0.2（供給+加工）· 再生成 `npm run excel:template` · 新規 `npm run excel:new-device` |
| `e2e/import-bundle.spec.ts` | import.json 取込 UI（DB 不要 · 実 Supabase 取込は Runbook §4 手動） |
| `lib/flowchart/validationMeta.ts` | 警告文言 · `WARNING_BANNER_HINT` |
| `components/flowchart/nodes/FlowShapeNode.tsx` | 縦並びラベル · 斜め図形 SVG · ID バッジ |
| `components/flowchart/exportImageFilter.ts` | PNG/SVG からプレビュー専用 DOM を除外 |
| `lib/flowchart/buildEdges.ts` | 順方向（`tierDiff > 0`）· ループ（`isLoop`）配線 |
